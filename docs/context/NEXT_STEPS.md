# Next Steps

*(Keep this intentionally short and update at the end of each session)*

1. Begin implementation of the core features outlined in `docs/context/CURRENT_PHASE.md` (Phase 5.3 - Reading Environment).
2. Establish the PDF rendering context and hook up `pdfjs-dist` ensuring isolation from the UI.
3. Validate architecture constraints (e.g., UI surrounds the page) in the initial scaffolding.
