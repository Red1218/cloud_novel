/**
 * Services layer.
 *
 * Phase 1: No services needed.
 *
 * Future phases will add:
 *  - bookService.ts   (CRUD operations for books)
 *  - syncService.ts   (cloud synchronisation)
 *  - storageService.ts (IndexedDB abstraction)
 */
export {};
