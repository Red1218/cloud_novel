/**
 * Application providers.
 *
 * Phase 1: No providers needed.
 *
 * Future phases will wrap the app with:
 *  - QueryClientProvider (React Query / data fetching)
 *  - ThemeProvider (multi-theme support)
 *  - etc.
 */
export {};
